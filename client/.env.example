# Only the API location is a build-time client setting. Google OAuth
# client ID, Neo4j credentials, and the rest are configured once via the
# in-app Setup Wizard (or server/.env) and served to the client at runtime
# from GET /api/setup/status — no client rebuild needed to change them.
VITE_API_BASE_URL=http://localhost:4000/api
